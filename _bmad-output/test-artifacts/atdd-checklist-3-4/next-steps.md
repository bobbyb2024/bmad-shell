# Next Steps

1. **Implement Story 3.4** — Run `bmad-dev-story` against `_bmad-output/implementation-artifacts/3-4-cycle-execution-engine.md`
2. **Green phase** — Remove `pytest.importorskip` and individual `@pytest.mark.skip` decorators as implementation progresses
3. **Refactor phase** — Clean up test helpers and shared fixtures if needed
